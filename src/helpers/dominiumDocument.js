import { ERRORS } from "../config/errors/error.constants.js";
import { ControllerHelper } from "./controller.js";
import { CardanoHelper } from "./cardano.js";

export const DominiumDocumentHelper = {
    createWrappedDocument: async (document) => {
        try {
        } catch (error) {
            throw error;
        }
    },
};
